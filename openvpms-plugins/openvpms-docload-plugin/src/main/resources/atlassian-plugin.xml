<atlassian-plugin key="${project.groupId}.${project.artifactId}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
    </plugin-info>

    <component-import key="archetypeService" interface="org.openvpms.component.business.service.archetype.IArchetypeService" />
    <component-import key="transactionManager" interface="org.springframework.transaction.PlatformTransactionManager" />
    <component-import key="scheduler" interface="org.quartz.Scheduler"/>

    <component key="documentLoader" class="org.openvpms.plugin.docload.DocumentLoaderPlugin">
        <description>Document Loader</description>
    </component>

</atlassian-plugin>